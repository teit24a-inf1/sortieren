package selectionsort

// SelectionSort sortiert eine Liste von Zahlen mit dem SelectionSort-Algorithmus.
func SelectionSort(list []int) {
	// HINWEIS:
	// Verwenden Sie eine For-Schleife, um über die Liste zu iterieren.
	// Rufen Sie SmallestPos für die Liste auf, beginnend mit der gesamten Liste
	// und dann nach und nach mit einer kürzeren Liste.
	// Tauschen Sie das kleinste Element mit dem ersten Element der Liste.

	// TODO
}
